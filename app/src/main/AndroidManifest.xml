<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.wilczak.freedivecomp.android">

    <application
        android:allowBackup="true"
        android:name=".application.FreediveApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >

        <activity android:name=".splash.SplashActivity" />
        <activity android:name=".startinglanes.StartingLanesActivity" />
        <activity android:name=".startinglist.StartingListActivity" />
        <activity android:name=".pairing.PairingActivity" />
        <activity android:name=".findrace.FindRaceActivity" />
        <activity android:name=".enterresult.EnterResultActivity" />

    </application>
</manifest>
